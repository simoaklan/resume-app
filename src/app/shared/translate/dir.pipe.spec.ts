/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DirPipe } from './dir.pipe';

describe('Pipe: Dir', () => {
  it('create an instance', () => {
    let pipe = new DirPipe();
    expect(pipe).toBeTruthy();
  });
});
